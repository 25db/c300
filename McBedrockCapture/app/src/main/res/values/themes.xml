<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.McBedrockCapture" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent</item>
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:windowBackground">@color/bg_glass_start</item>
    </style>

    <!-- 完全透明主题，用于 VPN 授权代理 Activity -->
    <style name="Theme.Transparent" parent="Theme.McBedrockCapture">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
